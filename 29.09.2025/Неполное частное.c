#include <stdio.h>
int main(void) {
    int a, b;
    int n = 0;
    int input = scanf("%d" "%d", &a, &b);
    while (a>=b (n+1)) {
        swap_array.c
        n++;
    }
    int resa-(b*n);
    if (n == 0) {
        printf("%d", 0);
    }
    else{
        printf("%d", res);
    }
    return 0;
}
